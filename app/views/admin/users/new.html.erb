<h1>New user</h1>

<%# form_tag :action => 'create' do %>
<%= form_for @user, :as => :user, :url => admin_users_url, :html => {:multipart => true} do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag "Create" %>
<% end %>

<%= link_to 'Back', admin_users_url %>